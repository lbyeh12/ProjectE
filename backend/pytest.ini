[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short
# backend/ (이 파일이 있는 위치)를 모듈 검색 경로에 추가.
# 이게 없으면 tests/conftest.py 의 `from app.database import ...` 가
# "No module named 'app'" 에러를 낸다 (uvicorn과 달리 pytest는
# 현재 폴더를 자동으로 sys.path 에 넣어주지 않기 때문).
pythonpath = .
